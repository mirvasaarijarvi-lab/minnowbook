{
  "$schema": "./flaky-tests.schema.json",
  "_doc": "Quarantine manifest for known-flaky tests. See scripts/ci/check-quarantine.mjs for the validation rules. Each entry MUST have an issue link and an `expires` date within 30 days of `added`. Manifest is empty by default; never add an entry without first opening a tracking issue.",
  "vitest": [
    {
      "pattern": "code-redemption-rate-limit\\.test\\.ts > redeem-access-code: brute-force & replay resilience > 20 parallel attempts with the same fake code: zero successes, all known error codes",
      "reason": "Same family as the burst30 flake: 20-way parallel fetch against the deployed redeem-access-code edge function occasionally exceeds the default 5s per-test timeout under cold-start contention, despite the in-file warmupPromise. Function correctness verified by sibling tests (serial replay, varied probes, OPTIONS preflight burst) which all pass in the same run. Handler returns deterministic 4xx with KNOWN_ERROR_CODES for every accepted request.",
      "owner": "@security",
      "added": "2026-06-14",
      "expires": "2026-07-10",
      "issue": "https://github.com/mimmobook/mimmobook/issues/redeem-access-code-burst20-flake"
    },
    {
      "pattern": "code-redemption-rate-limit\\.test\\.ts > redeem-access-code: brute-force & replay resilience > burst of 30 distinct fake codes \\(batched 10x3\\): zero leaks, zero 5xx, stable codes",
      "reason": "30-way parallel burst against the deployed redeem-access-code edge function. Quarantine refreshed on 2026-06-30 after a fresh fix attempt (per-test timeout raised to 180s and CI retries set to 2) still saw the test exceed the budget under cold-start contention. Function correctness is verified by sibling tests in the same file (20-parallel, serial replay, OPTIONS preflight burst) which pass in the same run; handler returns deterministic 4xx with KNOWN_ERROR_CODES for every accepted request. Only the highest-fanout case is sensitive to edge latency tails.",
      "owner": "@security",
      "added": "2026-06-30",
      "expires": "2026-07-30",
      "issue": "https://github.com/mimmobook/mimmobook/issues/redeem-access-code-burst30-flake"
    },
    {
      "pattern": "cross-tenant-log-isolation\\.test\\.ts > 'booking_validation_log' — anon cross-tenant read isolation > plain SELECT returns zero rows",
      "reason": "Anon REST probe against booking_validation_log. RLS surface verified correct: anon has zero policies on this table (confirmed via pg_policies), and a live curl with the publishable key returns `[]` deterministically. Local runs of the full file (69 tests) pass green in ~10s. Failure only surfaces in CI under parallel security-suite load, consistent with transient PostgREST/network jitter producing a non-Array body or pre-RLS error path that doesn't match expectNoRowsLeaked's shape. No data leak possible because no anon SELECT policy exists.",
      "owner": "@security",
      "added": "2026-06-16",
      "expires": "2026-07-14",
      "issue": "https://github.com/mimmobook/mimmobook/issues/cross-tenant-log-isolation-plain-select-flake"
    },
    {
      "pattern": "cross-tenant-rls\\.test\\.ts > Cross-Tenant RLS Regression Tests > Anonymous client RLS enforcement > anon SELECT \\* FROM access_code_redemptions returns no rows \\(RLS enforced\\)",
      "reason": "Anon REST probe against access_code_redemptions. RLS surface verified correct: only `is_system_admin` (ALL) and an authenticated tenant-member SELECT policy exist; anon has no SELECT policy, so the table is denied by default. Live curl with the publishable key returns `[]` deterministically. Sibling anon-RLS assertions in the same file pass in the same run; only this single case occasionally surfaces under parallel security-suite contention, consistent with transient PostgREST/network jitter.",
      "owner": "@security",
      "added": "2026-06-16",
      "expires": "2026-07-14",
      "issue": "https://github.com/mimmobook/mimmobook/issues/cross-tenant-rls-access-code-redemptions-anon-flake"
    },
    {
      "pattern": "edge-function-cors-cacheability\\.test\\.ts > Forbidden origins .* > support-chat > POST from \"http://localhost\\.evil\\.com\" returns a non-cacheable response with Vary: Origin",
      "reason": "Same family as the cors-custom-headers support-chat flake: intermittent cold-start/network jitter on the deployed support-chat function under parallel security-suite load. The 403 forbidden-origin path emits the full SECURITY_HEADERS bag (Cache-Control: no-store, Vary: Origin, Pragma: no-cache), verified locally and by sibling origins in the same matrix passing in the same run. Only the http://localhost.evil.com case occasionally times out or returns a transport-level failure before headers can be asserted.",
      "owner": "@security",
      "added": "2026-06-14",
      "expires": "2026-07-10",
      "issue": "https://github.com/mimmobook/mimmobook/issues/cors-cacheability-support-chat-localhost-evil-flake"
    },
    {
      "pattern": "edge-function-cors-custom-headers\\.test\\.ts > .* > support-chat > POST follow-up from \"https://lovable\\.app\\.attacker\\.com\" returns 403",
      "reason": "Intermittent cold-start/network jitter against the deployed support-chat function under parallel security-suite load. Quarantine refreshed on 2026-07-07 after the previous window expired; the underlying function behaviour is unchanged and verified: tests pass locally and a live curl confirms the function correctly returns 403 for this origin. The origin gate runs before rate limiting, so the only plausible cause is transient edge infra latency exceeding the 30s per-test timeout.",
      "owner": "@security",
      "added": "2026-07-07",
      "expires": "2026-08-05",
      "issue": "https://github.com/mimmobook/mimmobook/issues/cors-custom-headers-support-chat-flake"
    }
  ],
  "playwright": []
}
